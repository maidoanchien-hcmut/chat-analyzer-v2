{
  "name": "chat-analyzer-v2-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/dev-server.ts",
    "start": "bun src/dev-server.ts",
    "build": "bun run src/build.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "walkthrough:http": "bun run scripts/http-flow-walkthrough.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
